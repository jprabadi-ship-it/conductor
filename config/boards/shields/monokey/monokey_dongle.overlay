#include "monokey.dtsi"

/* * 【自爆回避のための完全ミラーストラテジー】
 * ダミーマトリクスを使うのをやめ、設計図をそのまま読み込みます。
 * 物理スイッチが繋がっていなくても、OSは「キーが押されていないだけ」と判断するため
 * 起動時のパニックを完全に防ぐことができます。
 */

&xiao_serial { status = "disabled"; };
